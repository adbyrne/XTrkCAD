# build unit tests for the xtrkcad library
#

# dxf formatting

add_executable(dxfformattest
				dxfformattest.c
				../dxfformat.c
			  )

target_include_directories(dxfformattest PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/..)

target_link_libraries(dxfformattest
                      PRIVATE
                      dynstring
					 ${LIBS})

add_test(DXFOutputTest dxfformattest)

# GetArgs 'q' quoted-field round-trip against the real escaping functions
# (misc.c) -- see getargstest.c's header comment. Links getargs.c/mymalloc.c
# directly (matching fuzz_getargs.c's already-established minimal-dependency
# extraction) plus misc.c as a separate compiled source (not #include'd) so
# its own real ConvertToEscapedText()/ConvertFromEscapedText() get linked in.

add_executable(getargstest
				getargstest.c
				../misc.c
				../mymalloc.c
				)

target_include_directories(getargstest PRIVATE
	${CMAKE_CURRENT_SOURCE_DIR}/..
	${CMAKE_CURRENT_SOURCE_DIR}/../include
	${CMAKE_SOURCE_DIR}/app/wlib/include
	${messagefile_BINARY_DIR}
)

# misc.c #includes common-ui.h -> messages.h, generated by the msgfiles
# target (see app/help/CMakeLists.txt) -- same dependency app/bin's own
# xtrkcad-lib target declares.
add_dependencies(getargstest msgfiles)

# misc.c is NOT isolated the way getargs.c is -- it contains wMain(), the
# whole application's entry point, transitively referencing ~100 other
# symbols (menu/toolbar/undo/pref/audio/splash-screen init, etc.) that have
# nothing to do with ConvertToEscapedText()/ConvertFromEscapedText(), the
# only two functions this test actually needs from it. Since nothing here
# calls wMain(), --gc-sections (with -ffunction-sections/-fdata-sections so
# the linker can discard at per-function granularity) lets the linker
# eliminate wMain()'s entire unreachable call graph instead of requiring
# ~100 stub functions -- same technique fuzz_getargs.c's header comment
# describes using for the analogous fileio.c extraction. This only works if
# ENABLE_EXPORTS is turned back off for this one target -- PlatformSettings.cmake
# sets CMAKE_ENABLE_EXPORTS TRUE globally (every executable gets
# --export-dynamic/-rdynamic, for plugin/backtrace symbol resolution),
# which forces every global symbol "live" and defeats --gc-sections
# entirely; found empirically when wMain() stayed linked despite nothing
# calling it.
set_target_properties(getargstest PROPERTIES ENABLE_EXPORTS FALSE)
target_compile_options(getargstest PRIVATE -ffunction-sections -fdata-sections)
if(APPLE)
	# Apple's ld64 doesn't understand GNU's --gc-sections syntax at all
	# ("ld: unknown options: --gc-sections", confirmed on the macOS CI
	# runner) -- -dead_strip is its equivalent flag for the same
	# per-symbol dead-code elimination.
	target_link_options(getargstest PRIVATE -Wl,-dead_strip)
else()
	target_link_options(getargstest PRIVATE -Wl,--gc-sections)
endif()

target_link_libraries(getargstest ${LIBS})

add_test(GetArgsTest getargstest)

# path manipulation

add_executable( pathstest
				pathstest.c
				)

# pathstest.c #includes ../paths.c directly; same vendored uthash.h
# macro-attribution exemption as the real paths.c compilation unit in
# app/bin/CMakeLists.txt (Phase 12, SF #663). CMAKE_C_COMPILER_ID is
# "AppleClang" (not "Clang") for Xcode/macOS Clang -- match both.
if(CMAKE_C_COMPILER_ID MATCHES "Clang")
    set_source_files_properties(pathstest.c PROPERTIES COMPILE_OPTIONS "-Wno-error=implicit-fallthrough")
endif()

target_include_directories(pathstest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(pathstest
                      PRIVATE
                      dynstring
					  xtrkcad-wlib
					  ${LIBS})

add_test(PathsTest pathstest)

add_executable(shortentest
			  shortentest.c
			  ../shortentext.c
			 )

target_include_directories(shortentest PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/..)

target_link_libraries(shortentest
					PRIVATE
					${LIBS})

add_test(ShortenTest shortentest)

# app defaults

add_executable(defaultstest
			  defaultstest.c
			  )

target_include_directories(defaultstest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(defaultstest
					  ${LIBS})

add_test(DefaultsTest defaultstest)

# geometric utility functions

add_executable(utilitytest
			  utilitytest.c
			  )

target_include_directories(utilitytest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(utilitytest ${LIBS})
if(UNIX)
    target_link_libraries(utilitytest m)
endif()

add_test(UtilityTest utilitytest)

# Bezier pure-math functions

add_executable(beziertest
			  beziertest.c
			  )

target_include_directories(beziertest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(beziertest ${LIBS})
if(UNIX)
    target_link_libraries(beziertest m)
endif()

add_test(BezierTest beziertest)

# Cornu pure-math functions

add_executable(cornutest
			  cornutest.c
			  )

target_include_directories(cornutest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(cornutest ${LIBS})
if(UNIX)
    target_link_libraries(cornutest m)
endif()

add_test(CornuTest cornutest)

# undo stream buffer operations

add_executable(undostreamtest
	undostreamtest.c
)

target_include_directories(undostreamtest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(undostreamtest ${LIBS})

add_test(UndoStreamTest undostreamtest)

# cars/ pure functions (rolling-stock dialog rework, Phase A)

add_executable(carstest
	carstest.c
	../cars/tabstring.c
	../cars/transform_pts.c
)

target_include_directories(carstest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_CURRENT_SOURCE_DIR}/../cars
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(carstest PRIVATE ${LIBS})
if(UNIX)
	target_link_libraries(carstest PRIVATE m)
endif()

add_test(CarsTest carstest)

# cars/ registry functions, linked directly (rolling-stock dialog rework, Phase H)

add_executable(carregistrytest
	carregistrytest.c
	../cars/carproto.c
	../cars/carpart.c
	../cars/listelem.c
	../cars/tabstring.c
	../cars/transform_pts.c
)

target_include_directories(carregistrytest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_CURRENT_SOURCE_DIR}/../cars
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(carregistrytest PRIVATE ${LIBS})
if(UNIX)
	target_link_libraries(carregistrytest PRIVATE m)
endif()

add_test(CarRegistryTest carregistrytest)

# cars/careditlogic.c -- pure validation/resolution helpers used by the car
# edit dialog's Ok-gating and commit-time checks (rolling-stock dialog
# rework). Only external dependency is GetScaleRatio (scale.c), stubbed in
# the test file -- no gc-sections trick needed here.

add_executable(careditlogictest
	careditlogictest.c
	../cars/careditlogic.c
)

target_include_directories(careditlogictest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_CURRENT_SOURCE_DIR}/../cars
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(careditlogictest PRIVATE ${LIBS})
if(UNIX)
	target_link_libraries(careditlogictest PRIVATE m)
endif()

add_test(CarEditLogicTest careditlogictest)

# reports.c's pure row-formatting function (Reports feature, SF #217 phase
# 1), split into reportsformat.c so this target stays a small pure-logic
# link like careditlogictest, instead of stubbing reports.c's wlib/track-
# database dependency surface the way carregistrytest.c has to.

add_executable(reportstest
	reportstest.c
	../reportsformat.c
)

target_include_directories(reportstest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(reportstest PRIVATE dynstring ${LIBS})

add_test(ReportsTest reportstest)

# elevjoin.c -- pure endpoint elevation-mode merge logic extracted from
# elev.c:SetTrkElevModes(). No dependency beyond libc and the ELEV_*
# constants in track.h, so it links as the real source with no stubs.

add_executable(elevjointest
	elevjointest.c
	../elevjoin.c
)

target_include_directories(elevjointest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(elevjointest PRIVATE ${LIBS})
if(UNIX)
	target_link_libraries(elevjointest PRIVATE m)
endif()

add_test(ElevJoinTest elevjointest)

# Layer Groups (SF #222 phase 0, SF #782) -- pure group registry/data-model
# logic, extracted into dlayergroup.c so it carries no dependency on
# common.h/dynarray.h/wlib, same rationale as reportsformat.c/
# cars/careditlogic.c.

add_executable(layergrouptest
	layergrouptest.c
	../dlayergroup.c
)

target_include_directories(layergrouptest PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CMAKE_SOURCE_DIR}/app/wlib/include
)

target_link_libraries(layergrouptest PRIVATE ${LIBS})

add_test(LayerGroupTest layergrouptest)

set_target_properties(
	dxfformattest
	shortentest
	defaultstest
	pathstest
	utilitytest
	beziertest
	cornutest
	undostreamtest
	carstest
	carregistrytest
	careditlogictest
	reportstest
	elevjointest
	layergrouptest
 	PROPERTIES FOLDER UnitTests
 )

if(XTRKCAD_SANITIZE AND UNIX)
	set(SANITIZE_FLAGS -fsanitize=address,undefined -fno-omit-frame-pointer)
	foreach(t dxfformattest shortentest defaultstest pathstest utilitytest beziertest cornutest undostreamtest carstest carregistrytest careditlogictest reportstest elevjointest layergrouptest)
		target_compile_options(${t} PRIVATE ${SANITIZE_FLAGS})
		target_link_options(${t} PRIVATE ${SANITIZE_FLAGS})
	endforeach()
endif()

 if(WIN32 AND CMOCKA_DLL)
	add_custom_command(
		TARGET dxfformattest POST_BUILD
				COMMAND ${CMAKE_COMMAND} -E copy
				"${CMOCKA_DLL}"
				${CMAKE_CURRENT_BINARY_DIR}
	)
 endif()
